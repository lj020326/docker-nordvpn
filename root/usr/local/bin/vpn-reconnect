#!/command/with-contenv bash
# shellcheck shell=bash

set -eu

. /usr/local/bin/backend-functions

log "Starting VPN reconnection"

log "Stopping NordVPN service"
s6-rc stop svc-nordvpn
sleep 2
log "Starting NordVPN service"
s6-rc start svc-nordvpn

log "VPN reconnection completed"
exit 0
